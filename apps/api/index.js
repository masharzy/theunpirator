// Temporary Vercel entry point. Delete this file when the API moves to a VPS.
// Keep this entry point tied to the current control API build so Vercel redeploys
// backend route changes, including GET /v1/security/events/:id.
export { default } from "./src/app.js";

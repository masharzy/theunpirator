"use client";
import { MonitorSmartphone, RotateCcw, ShieldOff, ShieldCheck } from "lucide-react";
import { useEffect,useState } from "react";
import { api } from "@/lib/api";
import { Button } from "@/components/ui/button";
import { EmptyState, PageHeader, StatusPill, Surface } from "@/components/console-kit";
export default function ViewersPage(){const[users,setUsers]=useState([]),[selected,setSelected]=useState(null),[message,setMessage]=useState("");const load=()=>api("/v1/security/users").then(d=>setUsers(d.items||[]));useEffect(()=>{load().catch(e=>setMessage(e.message))},[]);async function open(id){try{setSelected(await api(`/v1/security/users/${id}`))}catch(e){setMessage(e.message)}}async function action(path){try{await api(path,{method:"POST"});setMessage("Security action applied.");await load();if(selected?.viewer?.id)await open(selected.viewer.id)}catch(e){setMessage(e.message)}}return <div className="space-y-8"><PageHeader eyebrow="Audience control" title="Viewers & devices" description="Inspect viewer identities, authorized devices and reset access when a learner changes devices."/>{message&&<div className="rounded-2xl border border-[#dce3d4] bg-white p-4 text-sm text-[#52604b]">{message}</div>}{users.length===0?<EmptyState title="No viewers yet" description="Viewer identities appear after your backend creates protected playback sessions."/>:<div className="grid gap-5 xl:grid-cols-[.8fr_1.2fr]"><Surface className="overflow-hidden"><div className="divide-y divide-[#edf0e9]">{users.map(u=><button key={u.id} onClick={()=>open(u.id)} className="flex w-full items-center justify-between gap-3 p-5 text-left hover:bg-[#fafbf8]"><div><p className="font-semibold">{u.displayLabel||u.externalUserId}</p><p className="mt-1 font-mono text-[11px] text-[#87917f]">{u.externalUserId}</p></div><StatusPill status={u.status}/></button>)}</div></Surface><Surface className="p-6">{selected?<><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#71805b]">Viewer detail</p><h2 className="mt-2 text-xl font-semibold">{selected.viewer.displayLabel||selected.viewer.externalUserId}</h2></div><div className="flex gap-2"><Button size="sm" variant="outline" onClick={()=>action(`/v1/security/users/${selected.viewer.id}/${selected.viewer.status==="blocked"?"unblock":"block"}`)}>{selected.viewer.status==="blocked"?<ShieldCheck size={14}/>:<ShieldOff size={14}/>} {selected.viewer.status==="blocked"?"Unblock":"Block"}</Button><Button size="sm" onClick={()=>action(`/v1/security/users/${selected.viewer.id}/devices/reset`)}><RotateCcw size={14}/>Reset devices</Button></div></div><div className="mt-6 space-y-3">{selected.devices?.length?selected.devices.map(d=><div key={d.id} className="rounded-2xl border border-[#e0e5d9] p-4"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-xl bg-[#edf5d8] text-[#536b31]"><MonitorSmartphone size={16}/></span><div><p className="font-medium">{d.deviceName||d.externalDeviceId}</p><p className="text-xs text-[#7d8876]">{d.browser||"Unknown browser"} · {d.os||"Unknown OS"}</p></div></div><StatusPill status={d.status}/></div><div className="mt-3 flex gap-2"><Button size="sm" variant="outline" onClick={()=>action(`/v1/security/devices/${d.id}/${d.status==="blocked"?"unblock":"block"}`)}>{d.status==="blocked"?"Unblock":"Block"}</Button></div></div>):<p className="text-sm text-[#7b8574]">No devices for this viewer.</p>}</div></>:<p className="text-sm text-[#7b8574]">Select a viewer to inspect devices and sessions.</p>}</Surface></div>}</div>}

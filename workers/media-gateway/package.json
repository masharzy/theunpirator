{
  "name": "@unpirator/media-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:node": "node --watch src/node-server.js",
    "start:node": "node src/node-server.js",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@unpirator/cache": "workspace:*",
    "@unpirator/provider-youtube-custom": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.33.0"
  }
}
